# 🔄 완전한 양방향 워크플로우 가이드

Claude에게 명령 → macOS 앱으로 확인 → 결정 입력 → Claude가 실행 → 다시 확인

---

## 🎯 완성된 시스템

```
      ┌───────────────────┐
      │   1. Claude CLI   │
      │   명령 실행       │
      └─────────┬─────────┘
                │
                ↓ JSON 업데이트
      ┌───────────────────┐
      │  2. JSON 파일     │
      │  데이터 저장소    │
      └─────────┬─────────┘
                │
                ↓ 파일 감시
      ┌───────────────────┐
      │  3. macOS 앱      │
      │  시각화 + 입력    │
      └─────────┬─────────┘
                │
                ↓ 결정/요구사항 입력
      ┌───────────────────┐
      │  4. Queue JSON    │
      │  decisions-queue  │
      │  requests-queue   │
      └─────────┬─────────┘
                │
                ↓ 큐 처리
      ┌───────────────────┐
      │  5. Claude CLI    │
      │  자동 실행        │
      └───────────────────┘
                │
                └─→ 1번으로 순환
```

---

## 📚 전체 파일 구조

```
app-portfolio/
├── 📊 데이터 (읽기/쓰기)
│   ├── portfolio-summary.json      # 전체 요약
│   ├── apps/*.json                 # 23개 앱 데이터
│   ├── decisions-queue.json        # ✨ CEO 결정사항 큐
│   ├── requests-queue.json         # ✨ CEO 요구사항 큐
│   └── ceo-feedback.json           # ✨ CEO 피드백
│
├── 🤖 Claude CLI 스크립트
│   ├── ceo-daily-briefing.sh       # 브리핑 생성
│   ├── ceo-weekly-review.sh        # 주간 리뷰
│   ├── process-decisions.sh        # ✨ 결정사항 처리
│   ├── process-requests.sh         # ✨ 요구사항 처리
│   └── ceo-process-all.sh          # ✨ 전체 처리 (마스터)
│
├── 📱 macOS 앱
│   ├── Models/                     # 데이터 모델
│   ├── Services/
│   │   ├── PortfolioService.swift  # JSON 읽기 + 파일 감시
│   │   ├── DecisionQueueService.swift # ✨ 결정 저장
│   │   └── RequestQueueService.swift  # ✨ 요청 저장
│   └── Views/
│       ├── DashboardView.swift     # 대시보드
│       ├── BriefingView.swift      # ✨ 브리핑 + 결정 UI
│       └── NewRequestView.swift    # ✨ 요구사항 입력
│
└── 📚 문서
    ├── CEO-OPERATION-SYSTEM.md     # 전체 시스템
    ├── CEO-WORKFLOW.md             # 양방향 워크플로우
    └── COMPLETE-WORKFLOW-GUIDE.md  # ← 이 문서
```

---

## 🚀 실전 시나리오: 완전한 하루

### 09:00 AM - 아침 브리핑

**1. 터미널에서 브리핑 생성**
```bash
cd ~/Documents/workspace/code/app-portfolio
./scripts/ceo-daily-briefing.sh
```

**Claude가 자동으로:**
- 모든 앱 데이터 분석
- 긴급 의사결정 사항 발견
- `reports/ceo-briefing-2026-01-17.md` 생성

**2. macOS 앱 자동 업데이트**
- 파일 변경 감지
- 브리핑 내용 표시
- 알림: "☀️ CEO 브리핑 준비 완료"

**3. 앱에서 확인 및 결정**
```
브리핑 화면:
┌──────────────────────────────────┐
│ 🚨 긴급 의사결정 필요            │
├──────────────────────────────────┤
│ [세끼] 식단 피드백 기능 구현     │
│                                  │
│ ○ 옵션 A: 간단한 텍스트 (1주)   │
│ ○ 옵션 B: AI 사진 분석 (4주)    │
│                                  │
│ 추천: 옵션 A (빠른 출시)         │
│                                  │
│ [메모 입력]                      │
│ ┌──────────────────────────────┐ │
│ │ 1주 안에 MVP 완성하고       │ │
│ │ 사용자 반응 보자             │ │
│ └──────────────────────────────┘ │
│                                  │
│  [옵션 A 선택]  [보류]          │
└──────────────────────────────────┘
```

**4. 버튼 클릭 → 자동 저장**
- `decisions-queue.json`에 추가:
```json
{
  "id": "dec-001",
  "type": "feature-decision",
  "appName": "세끼",
  "selectedOption": "A",
  "notes": "1주 안에 MVP 완성하고 사용자 반응 보자"
}
```

---

### 09:30 AM - 중간에 아이디어

**1. 대시보드 보다가 생각**
```
"두 번 알림" 카드에서
→ [메모 버튼] 클릭
```

**2. 빠른 메모 입력**
```
┌──────────────────────────────────┐
│ 빠른 메모 - 두 번 알림          │
├──────────────────────────────────┤
│ 유형: ○ 새 태스크               │
│       ○ 버그                     │
│       ● 메모                     │
│                                  │
│ [내용 입력]                      │
│ ┌──────────────────────────────┐ │
│ │ 위젯 다크모드 지원 추가      │ │
│ │ - 사용자 요청 많음           │ │
│ │ - iOS 18 가이드라인 참고     │ │
│ └──────────────────────────────┘ │
│                                  │
│ 우선순위: ○ 높음 ● 중간 ○ 낮음 │
│                                  │
│           [저장]                 │
└──────────────────────────────────┘
```

**3. 저장 → `requests-queue.json`에 추가**

---

### 14:00 PM - 위험 요소 발견

**1. 대시보드에 경고 표시**
```
⚠️ 주의 필요
┌──────────────────────────────────┐
│ 라포 맵 - 진행률 6%              │
│ 3주 연속 정체                    │
│                                  │
│  [조치하기]  [일시중단]  [무시] │
└──────────────────────────────────┘
```

**2. "일시중단" 클릭**
```
┌──────────────────────────────────┐
│ 라포 맵 일시 중단 확인           │
├──────────────────────────────────┤
│ 이유를 입력하세요:               │
│ ┌──────────────────────────────┐ │
│ │ 리소스를 완료 임박한 앱에    │ │
│ │ 집중하기 위해                 │ │
│ └──────────────────────────────┘ │
│                                  │
│    [확인]         [취소]         │
└──────────────────────────────────┘
```

**3. 확인 → `decisions-queue.json`에 추가**
```json
{
  "id": "dec-002",
  "type": "priority-change",
  "appName": "라포 맵",
  "action": "pause",
  "reason": "리소스를 완료 임박한 앱에 집중"
}
```

---

### 17:00 PM - 하루 마무리

**1. 터미널에서 모든 결정 실행**
```bash
./scripts/ceo-process-all.sh
```

**출력:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 CEO 워크플로우 전체 처리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  결정사항 큐 처리...
📋 CEO 결정사항 처리 중...

✅ dec-001 처리 완료
   - 세끼 앱에 "식단 피드백 MVP" 태스크 추가
   - targetVersion: 1.1.0
   - 예상 기간: 1주

✅ dec-002 처리 완료
   - 라포 맵 우선순위 → low
   - status → paused
   - notes 추가

2️⃣  요구사항 큐 처리...
📝 CEO 요구사항 처리 중...

✅ req-001 처리 완료
   - 두 번 알림에 "위젯 다크모드" 태스크 추가
   - priority: medium
   - targetVersion: 1.0.6

3️⃣  CEO 피드백 반영...
   ⏭️  건너뛰기 (피드백 없음)

4️⃣  포트폴리오 요약 재생성...
   ✅ 요약 생성 완료

5️⃣  대시보드 업데이트...
   ✅ 대시보드 업데이트 완료

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 모든 CEO 입력 처리 완료!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📱 macOS 앱에서 결과를 확인하세요.
```

**2. macOS 앱 자동 새로고침**
- 세끼: 새 태스크 "식단 피드백 MVP" 표시
- 라포 맵: 우선순위 low, "일시 중단" 배지
- 두 번 알림: 새 태스크 "위젯 다크모드"

**3. 결과 확인**
```
대시보드에서:
✅ 오늘 처리: 3개 결정, 1개 요청
✅ apps/*.json 업데이트됨
✅ 내일 브리핑에 반영됨
```

---

## 🎯 핵심 기능

### 1. 의사결정 (Decision)

**지원하는 결정 유형:**
- ✅ **feature-decision**: A/B 선택
- ✅ **priority-change**: 우선순위 변경, 일시중단
- ✅ **task-update**: 태스크 상태 변경

**macOS 앱에서:**
```swift
// 버튼 클릭 시
DecisionQueueService.shared.addDecision(CEODecision(
    type: "feature-decision",
    appName: "세끼",
    selectedOption: "A",
    notes: "빠르게 MVP"
))
```

**Claude가 처리:**
```bash
./scripts/process-decisions.sh
→ apps/세끼.json 업데이트
→ 새 태스크 추가
→ 완료 표시
```

### 2. 요구사항 (Request)

**지원하는 요청 유형:**
- ✅ **new-task**: 새 태스크 추가
- ✅ **bug-report**: 버그 리포트
- ✅ **note**: 간단한 메모

**macOS 앱에서:**
```swift
// 메모 버튼 클릭
RequestQueueService.shared.addQuickNote(
    appName: "두 번 알림",
    content: "위젯 다크모드 지원"
)
```

**Claude가 처리:**
```bash
./scripts/process-requests.sh
→ apps/두번알림.json에 태스크 추가
→ 우선순위 설정
→ targetVersion 지정
```

### 3. 피드백 (Feedback)

**주간 회고 및 목표:**
```json
{
  "weeklyGoals": [
    "두 번 알림 배포",
    "세끼 MVP 완성"
  ],
  "appFeedback": {
    "두 번 알림": {
      "priority": "increase",
      "note": "이번 주 최우선"
    }
  }
}
```

---

## 💡 실전 팁

### 매일
```bash
# 아침
./scripts/ceo-daily-briefing.sh
# macOS 앱에서 결정

# 저녁
./scripts/ceo-process-all.sh
# 결과 확인
```

### 매주
```bash
# 일요일 저녁
./scripts/ceo-weekly-review.sh
# 다음 주 전략 설정
# macOS 앱에서 피드백 입력
```

### 긴급 상황
```bash
# 즉시 처리
./scripts/process-decisions.sh
# 또는
./scripts/ceo-process-all.sh
```

---

## 📊 데이터 흐름

### 읽기 (Claude → macOS 앱)
```
Claude CLI → JSON 업데이트
    ↓
파일 감시 감지
    ↓
macOS 앱 자동 새로고침
    ↓
화면 업데이트
```

### 쓰기 (macOS 앱 → Claude)
```
macOS 앱 버튼 클릭
    ↓
Queue JSON 저장
    ↓
터미널에서 처리 스크립트
    ↓
Claude가 큐 읽고 실행
    ↓
apps/*.json 업데이트
    ↓
(다시 읽기 흐름으로)
```

---

## 🎊 완성!

이제 당신은:
- ✅ Claude에게 **명령**할 수 있습니다
- ✅ macOS 앱으로 **확인**할 수 있습니다
- ✅ 앱에서 **결정**을 내릴 수 있습니다
- ✅ Claude가 **자동 실행**합니다
- ✅ 결과를 다시 **확인**합니다

**완전한 순환 구조!** 🔄

---

## 🚀 시작하기

```bash
# 1. 큐 파일 생성
cd ~/Documents/workspace/code/app-portfolio
touch decisions-queue.json requests-queue.json ceo-feedback.json
echo '{"pendingDecisions":[],"completedDecisions":[]}' > decisions-queue.json
echo '{"requests":[]}' > requests-queue.json
echo '{}' > ceo-feedback.json

# 2. 스크립트 실행 권한
chmod +x scripts/process-*.sh scripts/ceo-process-all.sh

# 3. macOS 앱 빌드
open PortfolioCEO/PortfolioCEO.xcodeproj

# 4. 첫 사용
./scripts/ceo-daily-briefing.sh
./scripts/ceo-process-all.sh
```

---

**이제 진짜 CEO처럼 일하세요!**
**명령하고, 확인하고, 결정하고, 실행하고, 다시 확인하세요!** 👔✨
